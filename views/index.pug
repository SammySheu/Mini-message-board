extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
      .message-card
        h2= message.text
        footer
          p=message.user
          p=message.added
  a(href='/new')
    button Add another comment!
